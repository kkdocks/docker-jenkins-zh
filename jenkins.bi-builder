#!/usr/bin/env bash

echo ">>> $PWD"
docker run --rm -v /home/nfs/jenkins/jenkins_home/workspace/$(basename `pwd`):/home/superset registry.cn-shenzhen.aliyuncs.com/nekoimi-deploy/python:bi-builder python "$@"
